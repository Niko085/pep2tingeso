      apiVersion: v1
kind: PersistentVolume
metadata:
  name: dbmshistorial-pv
  labels:
    type: local
spec:
  storageClassName: standard
  capacity:
    storage: 1Gi
  accessModes:
    - ReadWriteOnce
  hostPath:
    path: "/data/dbmshistorial-data"
---
apiVersion: v1
kind: PersistentVolumeClaim
metadata:
  name: dbmshistorial-pvc
  labels:
    app: postgres
    tier: database
spec:
  storageClassName: standard
  accessModes:
    - ReadWriteOnce
  resources:
    requests:
      storage: 1Gi
---
apiVersion: apps/v1
kind: Deployment
metadata:
  name: dbmshistorial-deployment
  labels:
    app: dbmshistorial-deployment
    tier: database
spec:
  selector:
    matchLabels:
      app: dbmshistorial-deployment
  strategy:
    type: Recreate
  template:
    metadata:
      labels:
        app: dbmshistorial-deployment
        tier: database
    spec:
      containers:
        - name: dbmshistorial
          image: postgres
          imagePullPolicy: "IfNotPresent"
          ports:
            - containerPort: 5432
          env:
            - name: POSTGRES_USER
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_USER_REPARACIONES
            - name: POSTGRES_PASSWORD
              valueFrom:
                secretKeyRef:
                  name: postgres-credentials
                  key: DB_PASSWORD_REPARACIONES
          volumeMounts:
            - mountPath: /var/lib/postgresql/data
              name: dbmshistorial-data
      volumes:
        - name: dbmshistorial-data
          persistentVolumeClaim:
            claimName: dbmshistorial-pvc
---
apiVersion: v1
kind: Service
metadata:
  name: dbmshistorial-service
  labels:
    app: dbmshistorial-service
    tier: database
spec:
  ports:
    - port: 5432
      targetPort: 5432
      protocol: TCP
  selector:
    app: dbmshistorial-deployment
    tier: database
  type: LoadBalancer